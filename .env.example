# Copy to .env for LOCAL testing only (.env is gitignored).
# In GitHub, set these as repository secrets instead — see README step 3.

RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
MAIL_TO=you@example.com
MAIL_FROM=Jobs <alerts@yourdomain.com>

# Optional overrides:
# PAGE_SIZE=100
# MAX_PAGES=25
# DRY_RUN=1
